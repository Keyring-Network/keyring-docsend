# Dependencies
node_modules
.pnpm-store

# Next.js
.next
out
tsconfig.tsbuildinfo

# Local data store (json-file storage driver)
.data
.tmp-test

# Env
.env
.env.local
.env*.local

# Your document lives in public/content/. The sample is committed so the app
# runs out of the box; replace it with your own (and gitignore it there if you
# don't want your deck in version control).

# OS / editor
.DS_Store
*.log
.vscode
.idea

# Vercel
.vercel
